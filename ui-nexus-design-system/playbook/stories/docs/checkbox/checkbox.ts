const checkboxDoc = `
<p class="nexus-body primary-story">Checkbox is an input for choosing from predefined options, when used alone, it gives a binary choice (checked/unchecked), and when used in a group it allows the user to select multiple values from a list of options. They are used for multiple choices, not for mutually exclusive choices</p>
`;

export default checkboxDoc;