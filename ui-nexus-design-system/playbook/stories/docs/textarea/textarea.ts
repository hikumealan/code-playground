const textareaDoc = `
<p class="nexus-body primary-story">A textarea allow users to enter text into a UI.They typically appear in forms and dialogs.
The <b> nexus-textarea </b> component is a wrapper for a native textarea html element.
In order to use the <code>nexus-textarea</code> we first start with a <code> nexus-form-field </code>.This is the wrapper component for all text area elements.
Inside the nexus-form-field we can then add the <code>nexus-label</code> component and the nexus-textarea components to complete our setup.
</p>
`;
export default textareaDoc