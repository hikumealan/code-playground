const badgeDoc = `
<p class="nexus-body primary-story">A small label, generally appearing inside or in close proximity to another larger interface component, representing a status, property, or some other metadata. Badges often represent a value or status to a user.</p>
`;

export default badgeDoc;