const removableDoc = `
<p class="nexus-body primary-story">Iteractive example for a checkbox and chip. Closing of chip triggers the event "triggerRemovalClick". This event will update the checkbox status to unchecked.</p>
`;

export default removableDoc;