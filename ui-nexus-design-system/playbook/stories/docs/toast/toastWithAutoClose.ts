const toastWithAutoCloseDoc = `
<p class="nexus-body primary-story">The toast component has an attibute "auto-close" which allows users to configure auto closure for a given time intervel.
For example The auto close intervel is provided in terms of millisecond.

If the component is expected to auto close in 3 seconds set the attribute as "auto-close=3000"
</p>
`;

export default toastWithAutoCloseDoc;