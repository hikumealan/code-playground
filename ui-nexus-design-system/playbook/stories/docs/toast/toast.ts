const toastDoc = `
<p class="nexus-body primary-story">A toast is a floating, non-actionable label used to explain a user interface element or feature. The toast text wraps when the content is wider than the max-width.
The component provides a variety of props to configure tooltip behavior for these different use cases. This include properties like "autoClose", "type" and "position" of the toast.</p>
`;

export default toastDoc;