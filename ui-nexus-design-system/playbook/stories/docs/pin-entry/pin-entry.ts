const pinEntryDoc = `
<p class="nexus-body primary-story">The PinInput component is similar to the Input component, but is optimized for entering sequences of digits quickly.</p>

<p class="nexus-body primary-story">The most common use case of the pin input is for entering OTP or security codes.

Each input collects one character at a time. When a value is entered, focus is moved automatically to the next input, until all fields are filled.</p>
`;

export default pinEntryDoc;