const tabbedDoc = `<p class="nexus-body primary-story">The example show how to embed "icons" within a nexus-btn. The button component can be also used to create content switcher with appropriate event listeners.
There is also an example which shows a success state for a button. This is not native to nexus button, however this example shows how we can extend nexus-btn to specific needs of theming.</p>`;

export default tabbedDoc;