const readOnlyDoc = `
<p class="nexus-body primary-story">An input component with a read-only prop is a text input field that is designed to only display the input and not allow any editing.</p>
`;

export default readOnlyDoc;
