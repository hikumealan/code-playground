const chipElement = document.getElementById('removableChip');

const onTriggerRemovableClick = () => {
  // Do something
};

chipElement.addEventListener('triggerRemovableClick', () => {
  onTriggerRemovableClick();
});

