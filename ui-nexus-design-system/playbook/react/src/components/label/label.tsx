import React from 'react';

import { NexusLabel } from '@nexus/react';

const LabelComponent: React.FC = () => <NexusLabel
    data-testid="label-first-name">
    First name</NexusLabel>;

export default LabelComponent;
